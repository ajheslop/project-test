name: Pre-Commit
on:
  push:
    branches: [ add-pre-commit ]
  pull_request:
    branches: [ add-pre-commit ]
jobs:
  Setup-Env:
    runs-on: ubuntu-latest
    steps:
# Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      # Runs a single command using the runners shell
  Pre-Commit-Checks:
    runs-on: ubuntu-latest
    needs: Setup-Env
    steps:
      - run: echo "🐧 running pre-commit tests on a ${{ runner.os }} server hosted by GitHub!"
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: pre-commit/action@v2.0.3
  Slack-Notification:
    runs-on: ubuntu-latest
    needs: Pre-Commit-Checks
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - name: Custom Slack Action
        uses: Brymastr/slack-action@v1
      - uses: act10ns/slack@v1
        with:
          template: './template.json'
          status: ${{ job.status }}
          example_key1: example value 1
          steps: ${{ toJson(steps) }}
          channel: '#code-review'
        if: always()

# # if required for a private repository use:
#  - uses: actions/checkout@v2
#       with:
#         fetch-depth: 0

#         - uses: pre-commit/action@v2.0.3
#       with:
#         token: ${{ secrets.GITHUB_TOKEN }}
